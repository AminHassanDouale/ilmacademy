APP_NAME="ilmacademie"
APP_ENV=Production
APP_KEY=base64:p1D2tX6p2AO0MLfkVjWfnoSBbofCvedUhkK4tt7vC2Q=
APP_DEBUG=false
APP_URL=https://smartdalib.work
APP_TIMEZONE=UTC

# Debug Configuration
DEBUGBAR_ENABLED=false

# Logging Configuration
LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

# MySQL Database Configuration
DB_CONNECTION=mysql
DB_HOST=localhost
DB_PORT=3306
DB_DATABASE=ilmacademy
DB_USERNAME=root
DB_PASSWORD=


# Cache Configuration (File-based - no Redis needed)
CACHE_DRIVER=file
CACHE_PREFIX=ilmacademie_cache

# Session Configuration (File-based - no Redis needed)
SESSION_DRIVER=file
SESSION_LIFETIME=120
SESSION_ENCRYPT=true
SESSION_PATH=/
SESSION_DOMAIN=null

# Queue Configuration (Sync - no Redis needed)
QUEUE_CONNECTION=sync
QUEUE_FAILED_DRIVER=database

# Broadcasting Configuration
BROADCAST_DRIVER=log

# File Storage
FILESYSTEM_DISK=local

# Memory Cache (Optional - only if you want memcached)
# MEMCACHED_HOST=127.0.0.1

# Redis Configuration (Disabled - not needed)
# REDIS_CLIENT=phpredis
# REDIS_HOST=127.0.0.1
# REDIS_PASSWORD=null
# REDIS_PORT=6379
# REDIS_DB=0
# REDIS_CACHE_DB=1
# REDIS_PREFIX=ilmacademie:

# Mail Configuration
MAIL_MAILER=smtp
MAIL_HOST=mailpit
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="noreply@ilmacademie.com"
MAIL_FROM_NAME="${APP_NAME}"

# AWS Configuration
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

# Pusher Configuration
PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_HOST=
PUSHER_PORT=443
PUSHER_SCHEME=https
PUSHER_APP_CLUSTER=mt1

# Frontend Configuration
VITE_APP_NAME="${APP_NAME}"
VITE_APP_ENV="${APP_ENV}"
VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
VITE_PUSHER_HOST="${PUSHER_HOST}"
VITE_PUSHER_PORT="${PUSHER_PORT}"
VITE_PUSHER_SCHEME="${PUSHER_SCHEME}"
VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"

# Application Specific Settings
DEFAULT_PAGINATION_SIZE=50
MAX_PAGINATION_SIZE=1000
SEARCH_RESULTS_LIMIT=100
REPORT_TIMEOUT=300
EXPORT_CHUNK_SIZE=1000

# Security Settings
BCRYPT_ROUNDS=12
HASH_VERIFY=true

# API Configuration
API_VERSION=v1
API_PREFIX=api

# Data Privacy Settings
DATA_RETENTION_DAYS=2555    # 7 years
ANONYMIZE_AFTER_DAYS=3650   # 10 years
AUDIT_LOG_RETENTION_DAYS=2555

# MySQL Performance Settings
# These can be added to config/database.php if needed
# DB_CONNECT_TIMEOUT=60
# DB_STATEMENT_TIMEOUT=30000
